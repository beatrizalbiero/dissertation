\begin{figure}[ht!]
\centering
\begin{tikzpicture}
\node[punkt] (seq1) {Forma Base (RAD + VT)};
\node[punkt, right=40pt of seq1] (seq2) {Verbo Flexionado};

\path[tedge] (seq1) -- (seq2);

\node[punkt, below=40pt of seq2] (seq21) {estudo};

\node[punkt, left=40 pt of seq21] (seq12) {estuda};

\path[tedge] (seq12) -- (seq21);

\node[text, below=10 pt of seq21] (nada) {};
\end{tikzpicture}
\caption{Relação entre um Verbo na Forma Base e o Mesmo Flexionado} 
\label{fig:verbs}
\end{figure}