\begin{tikzpicture}
\node[input] (st) {$\hat{\mathbf{s}}_{t}$};
\node[input, below=35pt of st] (theta) {$\theta$};

\node[deterministic, above right=10pt and 40pt of st] (rt)  {$r_{t}$};
\node[deterministic, below right=10pt and 40pt of st] (pit) {$\pi$};

\node[input, right=90pt of st] (s_next) {$\hat{\mathbf{s}}_{t+1}$};

\node[deterministic, above right=10pt of rt] (logprob) {$\log p(\hat{\mathbf{s}}_{t+1}|\hat{\mathbf{s}}_{t},\mathbf{a}_{t})~\hat{Q}_{t+1}$};

\path[pedge] (theta)  -- (pit);
\path[pedge] (st) -- (pit);
% \path[pedge] (pit) -- (s_next);
% \path[pedge] (st) -- (s_next);
\path[pedge] (pit) -- node [right] {$\mathbf{a}_t$} (rt);
% \path[pedge] (st) -- (rt);

\path[pedge] (s_next) -- (logprob);
\path[pedge] (pit) -- (logprob);
\path[pedge] (st) edge [bend left] (logprob);
\end{tikzpicture}


% \begin{tikzpicture}
% \node[input] (theta) {$\theta$};
% \node[input, below=5pt of theta] (s_curr) {$\mathbf{s}_{t}$};
% \node[input, below=2pt of s_curr] (s_next) {$\mathbf{s}_{t+1}$};
% \node[input] (theta) {$\theta$};
% \node[deterministic, right=30pt of theta] (pi) {$\pi$};
% \node[deterministic, right=40pt of pi] (r) {$\gamma^{t}~r_{t}$};
% \node[deterministic, below right=30pt of pi] (logprob) {$\log p(\mathbf{s}_{t+1}|\mathbf{s}_{t},\mathbf{a}_{t})~\hat{Q}_{t+1}$};

% \path[pedge] (theta)  -- (pi);
% \path[pedge] (s_curr)  -- (logprob);
% \path[pedge] (s_next)  -- (logprob);
% \path[pedge] (pi) -- node[below, midway] {$\mathbf{a}_t$} (r);
% \path[pedge] (pi) -- (logprob);
% \end{tikzpicture}