\begin{figure}[H]
\centering
\begin{tikzpicture}
\node[punkt] (pred) {Predição\\$\hat{y}$};
\node[punkt, right=40pt of pred] (arr) {Aproximação\\$\hat{y}_{ap}$};
\node[punkt, right=40pt of arr] (dist) {Distância Euclidiana com Fones Possíveis};
\node[punkt, right=40pt of dist] (subs) {Substituição pelo Vetor do Fone mais Próximo};

\path[tedge] (pred) -- (arr);
\path[tedge] (arr) -- (dist);
\path[tedge] (dist) -- (subs);


\node[punkt, below=40 pt of pred] (pred1) {[0.8734, 0.235, 0.003, ...]};
\node[punkt, below=40pt of arr] (arr1) {[1.0, 1.0, 0.0, ...]};

\node[punkt,below=60pt of dist] (dist1) {[1.0, 0.0, 0.0, ...]};

\node[punkt,below=20pt of dist] (dist2) {[1.0, 0.0, 1.0, ...]};

\node[punkt,below=100pt of dist] (dist3) {[0.0, 1.0, 0.0, ...]};

\node[box2, below=40pt of subs] (p) {\textbf{p}};


\path[tedge] (pred1) -- (arr1);
\path[tedge] (arr1) -- (dist1);
\path[tedge] (arr1) -- (dist2);
\path[tedge] (arr1) -- (dist3);

\path[tedge] (dist1) -- (p);

\node[text, below=10 pt of arr1] (nada) {};



\end{tikzpicture}
\caption{Pós-Processamento do Modelo} 
\label{fig:outputs}
\end{figure}